// Destino do login e da nav. A tela de verdade — categorias, produtos, preço em reais —
// é a Task 5; aqui fica só o cabeçalho para a rota existir sob a guarda de sessão.
export default function MenuPage() {
  return <h1 className="text-2xl font-semibold">Cardápio</h1>;
}
